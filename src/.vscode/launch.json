{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceRoot}/../build/athi",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceRoot}/../build",
            "environment": [],
            "externalConsole": true,
            "MIMode": "lldb"
        }
    ]
}